<UserControl x:Class="ERPFlowItalia.Desktop.Views.WarehousesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataGrid x:Name="WarehousesGrid"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              Background="White">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
            <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="160"/>
            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
            <DataGridTextColumn Header="City" Binding="{Binding City}" Width="120"/>
            <DataGridTextColumn Header="Province" Binding="{Binding Province}" Width="100"/>
            <DataGridTextColumn Header="Postal Code" Binding="{Binding PostalCode}" Width="110"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
